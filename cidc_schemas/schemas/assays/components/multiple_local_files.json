{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "multiple_local_files",
  "title": "A list of local files type",
  "type": "object",
  "description": "A type to store a comma-separated list of local file paths.",
  "properties": {
    "file_path_list": {
      "$id": "local_file_path_list",
      "type": "string",
      "description": "A comma-separated list of paths to files on a user's computer."
    }
  },
  "required": []
}
